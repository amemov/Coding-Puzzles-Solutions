#include "solution.h"

#include <gmock/gmock.h>
#include <gtest/gtest.h>

/*
// Small input - from LeetCode
TEST(visited_nodes_test, small_input_0) {
  EXPECT_THAT(CountVisitedNodes(std::span<const int>({1, 2, 0, 0})),
              testing::ElementsAreArray({3, 3, 3, 4}));
}

// Antother small input - from LeetCode
TEST(visited_nodes_test, small_input_1) {
  EXPECT_THAT(CountVisitedNodes(std::span<const int>({1, 2, 3, 4, 0})),
              testing::ElementsAreArray({5, 5, 5, 5, 5}));
}

// Some good input - from LeetCode
TEST(visited_nodes_test, good_input) {
  EXPECT_THAT(CountVisitedNodes(std::span<const int>({8,  17, 14, 8, 14, 12, 16, 11, 4, 14,
                                 19, 6,  8,  8, 2,  10, 2,  1,  1, 18})),
              testing::ElementsAreArray({5, 2, 2, 5, 3, 6, 4, 6, 4, 3,
                                         5, 5, 5, 5, 2, 6, 3, 2, 3, 4}));
}
*/